<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Lets a debug build reach the API over plain HTTP on the emulator's view
         of the host machine. Scoped to two hosts, and merged only into debug
         builds — see the comment in the file it points at. `tools:replace` is
         required because the main manifest's <application> otherwise wins the
         merge and this attribute is dropped silently. -->
    <application
        android:networkSecurityConfig="@xml/network_security_config"
        tools:replace="android:networkSecurityConfig"/>
</manifest>
